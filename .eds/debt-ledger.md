# EDS debt ledger

Harvested `eds: deferred` markers — run `/eds-debt` to review, or clear an entry once addressed.

## 2026-07-11

- `tests/test_harvest_debt.py:44` — skipping cross-validation for speed\nprint(1)\n")
- `tests/test_harvest_debt.py:52` — holdout re-use marker")\n'
- `tests/test_harvest_debt.py:61` — reason marker."""\n'
- `tests/test_harvest_debt.py:69` — needs real DB connection\n")
- `tests/test_harvest_debt.py:76` — already harvested\n")
- `tests/test_harvest_debt.py:87` — double dash reason\n")
- `tests/test_harvest_debt.py:89` — single dash reason\n")
- `skills/mde/scripts/holdout_ledger.py:46` — holdout re-use'."
- `skills/fde/scripts/evaluators/funnel.py:222` — holdout re-use' marker, not a silent second look"
- `hooks/scripts/ds-lint.js:21` — needs a
